{
    "name": "pokemouille",
    "private": true,
    "workspaces": ["packages/*"],
    "scripts": {
        "dev": "bun --filter '*' dev",
        "lint": "bun --filter '*' lint",
        "lint:fix": "bun --filter --parallel '*' lint:fix",
        "fmt": "bun --filter '*' fmt",
        "fmt:check": "bun --filter '*' fmt:check"
    }
}
